bin/

# local scratch that is not part of the plugin: the publish review and its
# working notes. Ignored rather than deleted, since they are still useful here.
docs/publish-audit.html
docs/TODOs.md

# what the daemon writes, in case a state dir is pointed at the checkout
snapshot.json
state.json
ui.json
ui.json.lock
chain.json
keys.optout
musterd.log
musterd.log.1
musterd.lock
musterd.spawn.lock

# scratch: demo staging, screenshot working files
tmp/

# a personal toolchain pin, deliberately not tracked: see CONTRIBUTING.md
.mise.local.toml
